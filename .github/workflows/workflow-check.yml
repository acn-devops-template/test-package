name: workflow check
on:
  #push:
  #  branches: [ master ]
  #pull_request:
  #  branches: [ master ]
  workflow_dispatch:

jobs:
  Checkers:

    runs-on: ${{ matrix.runner }}
    strategy:      
      matrix:        
        runner:
          - self-hosted-ai-c360dev
          - self-hosted-ai-c360stg
          - self-hosted-ai-c360prd
          - self-hosted-dev

    env:
      CHECKERS: --webapp-security

    steps:
    - name: Connect to AzDevOps
      run: |
        hostname
        dig pkgs.dev.azure.com
        curl -vvv https://pkgs.dev.azure.com

